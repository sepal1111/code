# 找到寶藏吧！邏輯冒險 (Find the Treasure! Logic Adventure)

這是一個專為教育設計的網頁程式邏輯遊戲，旨在透過趣味的航海冒險，培養學生的運算思維（Computational Thinking）。玩家需要透過組合指令卡片，引導船隻在多變的海域中避開障礙、善用傳送門，最終精準抵達寶藏位置。

## 🎮 遊戲特色

- **七大挑戰關卡**：精心設計的冒險模式，從基礎移動到「時空金鑰」等複雜機制，難度循序漸進。
- **全方位功能模組**：
  - **冒險模式**：挑戰內建關卡，解鎖「傳奇航海家」成就。
  - **地圖編輯器**：直觀的視覺化編輯工具，支援自訂障礙、傳送門與起始點，且可對每項指令進行「啟用/停用」及「數量限制」設定。
  - **自訂挑戰**：匯入編輯器產生的地圖代碼，與朋友分享或挑戰創意地圖。
  - **教學指南**：內建詳細的圖文操作說明。
- **豐富的程式邏輯元件**：
  - **指令卡片**：前進、後退、左轉、右轉、射擊（移除漂流木）。
  - **重複卡片**：x2、x3 及**自訂重複次數**的包裹區塊設計，支援輸入 1-99 次重複，並支援多層巢狀迴圈，學習迴圈概念。
  - **資源限制**：特定關卡限制指令數量，優化程式效率。
- **動態地圖機制**：
  - **障礙物 (礁石)**：需繞道而行的固定阻礙。
  - **傳送門 (渦流)**：空間跳躍的捷徑。
  - **金鑰系統**：需先取得鑰匙才能打開特定的寶藏箱。
- **📋 進度代碼系統**（新功能）：
  - 過關後自動記錄該關使用的積木序列。
  - 可將完整闖關進度（解鎖關卡 + 每關積木）壓縮為一段文字代碼，方便複製、下載或分享。
  - 匯入代碼後立即還原進度，並**完全覆蓋**瀏覽器現有紀錄，無縫跨裝置延續學習。
- **🏆 過關紀錄唯讀模式**（新功能）：
  - 進入已過關的關卡時，程式序列自動載入過關積木並**鎖定編輯**，以金色橫幅提示。
  - 仍可執行再次觀察路徑；點擊「清除全部」即可解鎖重新設計。
  - 匯入進度代碼後同樣支援此唯讀顯示。
- **👨‍🏫 教師評分管理**（新功能）：
  - 專屬的教師儀表板，支援輸入密碼進入。
  - 密碼格式為：code + 月日（各兩位數），例：code0518
  - 批量匯入學生的進度代碼，自動解析為包含班級、座號、通關數與各關積木數的總表。
  - 點擊「查看」自動跳轉至該學生的遊戲紀錄，還原通關路徑。
  - 支援一鍵匯出全班成績為 CSV 檔案。
- **現代化遊戲體驗**：
  - **自動擷取**：過關自動下載成果圖，方便學習記錄。
  - **影音沈浸**：豐富的背景音樂（BGM）與精確的音效反饋。
  - **響應式設計**：支援桌面與平板裝置。

## 🕹️ 如何遊玩

1. **選擇船隻**：在首頁選擇喜愛的頭像，這將作為您的執行角色。
2. **冒險/自訂**：選擇進入「闖關冒險」或「自訂挑戰」。
3. **編寫程式**：
   - 點擊藍色/紫色卡片添加動作。
   - 點擊橘色重複卡片，並透過「+」/「-」調整包裹範圍，將後續指令納入迴圈。使用自訂重複卡片時，可直接於卡片內輸入或調整重複次數（1-99 次）。
4. **執行與驗證**：
   - 點擊「🚀 開始航行」看著船隻依序執行。
   - **獲勝條件**：船隻必須在所有指令執行完畢時，**剛好停在寶藏（💰）位置**。
   - 若有金鑰要求，需先經過金鑰格。
5. **儲存與分享進度**：
   - 在左上角輸入「班級/座號」。
   - 點擊頂部「📋 進度代碼」按鈕，匯出當前進度代碼儲存或傳給同學/老師。
   - 收到別人的代碼時，在同一視窗切換至「匯入」頁籤貼上即可還原紀錄。

## 🛠️ 技術堆疊

- **核心**：HTML5, JavaScript (Vanilla ES6+)。
- **樣式**：Tailwind CSS (CDN 版本)。
- **工具套件**：
  - `dom-to-image`: 用於實現遊戲畫面擷取功能。
  - `Google Fonts`: 提供 Noto Sans TC 質感字體。
- **狀態管理**：使用 `localStorage` 持久化解鎖進度、每關積木紀錄、音量設定、自訂地圖及教師評分資料。
- **進度代碼格式**：Base64-URL 編碼的 JSON，包含版本號、解鎖關卡索引、全通旗標、班級座號、各關積木序列（精簡 ASCII 格式，如 `RN4` 代表自訂重複 4 次，`RN4w3` 代表自訂重複 4 次並包裹後續 3 個指令，`R2w2` 代表重複 2 次包裹 2 個指令）。

## 📁 檔案結構

- `index.html`: 遊戲入口與角色選擇頁面。
- `adventure.html`: 闖關冒險模式（包含 7 個主要關卡）。
- `editor.html`: 地圖編輯器工具。
- `custom.html`: 自訂地圖挑戰頁面。
- `tutorial.html`: 遊戲說明與指南。
- `teacher.html`: 教師評分管理儀表板。
- `music/`: 音效與背景音樂資源。

## 📝 更新紀錄

| 版本 | 日期 | 內容 |
|------|------|------|
| V3.1 | 2026-05-20 | 新增自訂重複次數指令（可自訂 1-99 次），地圖編輯器支援所有指令個別啟用與數量限制（含重複指令） |
| V3.0 | 2026-05-18 | 新增教師評分系統、進度代碼防篡改機制、重置資料確認視窗 |
| V2.4.0 | 2026-05-18 | 新增進度代碼（匯出/匯入/分享）、過關關卡積木唯讀鎖定模式 |
| V2.3.3 | 2026-05-12 | 修正關卡解鎖顯示、重複卡片多層巢狀架構重構 |
| V2.0.0 | 2026-05-12 | 冒險模式獨立為 adventure.html，新增第七關時空金鑰 |

---
*本專案靈感來自程式邏輯桌遊《Coding Ocean：海霸》。祝您航海愉快！*
